---
cluster_cidr: "{{ (server_config_yaml | from_yaml)['cluster-cidr'] | default('192.168.0.0/16') }}"  # noqa var-naming[no-role-prefix]
service_cidr: "{{ (server_config_yaml | from_yaml)['service-cidr'] | default('192.168.0.0/16') }}"  # noqa var-naming[no-role-prefix]
